# Logging

현재 저장소에는 중앙 로거와 관측 플랫폼이 없습니다. 브라우저 console 로그를 임의로 추가하지 않으며 오류는 사용자 상태와 query error 경계에서 처리합니다.

향후 로깅을 도입할 때는 안정된 event name과 구조화된 field를 사용하고 토큰, 비밀번호, 전체 API payload를 기록하지 않습니다. 로깅 도입은 공급자와 개인정보 정책을 포함한 ADR이 필요합니다.
